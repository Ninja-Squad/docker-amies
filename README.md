# Docker image for amies project

This is a docker image used by Gitlab CI to build the project, including:

- a Java base image
- a Chrome binary
- a PostgreSQL client

This image is available on [DockerHub](https://hub.docker.com/r/ninjasquad/docker-amies/).
